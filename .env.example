# Copy to .env and fill in.
#
# Sent as a Bearer token to the BoardGameGeek XML API (https://boardgamegeek.com/xmlapi2).
# Without it the board game pages will render, but the plays request will fail.
VITE_BGG_AUTH_TOKEN=
